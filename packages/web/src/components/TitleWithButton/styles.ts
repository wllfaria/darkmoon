import styled from 'styled-components'

export const STitleWithButton = styled.div`
	display: flex;
	flex: 1;
	justify-content: space-between;
	align-items: center;
	margin-bottom: ${props => props.theme.margins[1]};
`
