import Sku from "../../models/v1/sku.model";

export default class AddressController {
    public get = async (_req: Request, _res: Response) => {
        try { 

        } catch (e) {

        }
    }
}